import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { ArrowLeft, Building2, CheckCircle2, MapPin, Wrench } from "lucide-react"; import { CTA, Eyebrow, ReferenceImage } from "@/components/Shared"; import { projects } from "@/data/projects";
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=projects.find(x=>x.slug===slug);return {title:p?.title||"Project",description:p?.description}}
export default async function ProjectDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)notFound();return <><section className="project-hero"><div className="container"><Link href="/projects" className="back"><ArrowLeft/>Back to projects</Link><Eyebrow>{p.category}</Eyebrow><h1>{p.title}</h1><p>{p.description}</p></div></section><section className="section"><div className="container project-detail"><ReferenceImage alt={`${p.title} elevator`} position={p.imagePosition}/><div className="project-facts"><div><Building2/><small>Building type</small><b>{p.category}</b></div><div><Wrench/><small>Service provided</small><b>{p.service}</b></div><div><MapPin/><small>Location</small><b>{p.location}</b></div><div><CheckCircle2/><small>Status</small><b>{p.status}</b></div></div><div className="detail-copy"><div><Eyebrow>Main challenge</Eyebrow><h2>THE PROJECT NEED</h2><p>{p.challenge}</p></div><div><Eyebrow>Our approach</Eyebrow><h2>THE SOLUTION</h2><p>{p.solution}</p></div></div><p className="placeholder-note">This is a structured placeholder project. Images and final project details can be replaced in the central project data file.</p></div></section><CTA/></>}
