import type { Metadata } from "next"; import Link from "next/link"; import { ArrowRight, Check } from "lucide-react"; import { CTA, Eyebrow, PageHero, ReferenceImage } from "@/components/Shared"; import { services } from "@/data/services";
export const metadata:Metadata={title:"Elevator Services",description:"Professional elevator installation, repair, maintenance, modernization, and safety inspection services."};
export default function Services(){return <><PageHero title="ELEVATOR SERVICES" description="Professional installation, repair, maintenance, and modernization services."/><section>{services.slice(0,4).map(({icon:Icon,...s},i)=><article className={`service-detail ${i%2?"reverse":""}`} id={s.slug} key={s.slug}><div className="container split"><ReferenceImage alt={`${s.title} by Gentel Elevators`} position={s.imagePosition}/><div><Icon className="large-icon"/><Eyebrow>Service {String(i+1).padStart(2,"0")}</Eyebrow><h2>{s.title.toUpperCase()}</h2><p>{s.description}</p><div className="check-grid">{s.items.map(x=><span key={x}><Check/>{x}</span>)}</div><Link className="button button-gold" href={`/contact?service=${s.slug}#quote`}>Request a quotation<ArrowRight/></Link></div></div></article>)}</section><CTA/></>}
