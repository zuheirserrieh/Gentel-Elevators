import Link from "next/link"; import { Mail, MapPin, Phone } from "lucide-react"; import { Logo } from "./Logo"; import { companyInfo, navItems } from "@/data/company"; import { services } from "@/data/services";
export function Footer(){return <footer className="footer"><div className="container footer-grid"><div><Logo/><p>Professional elevator installation, repair, maintenance, and modernization for residential and commercial buildings.</p></div><div><h3>Explore</h3>{navItems.map(n=><Link key={n.href} href={n.href}>{n.label}</Link>)}</div><div><h3>Services</h3>{services.slice(0,4).map(s=><Link key={s.slug} href={`/services#${s.slug}`}>{s.title}</Link>)}</div><div><h3>Contact</h3><a href={`tel:${companyInfo.phone}`}><Phone size={15}/>{companyInfo.phone}</a><a href={`mailto:${companyInfo.email}`}><Mail size={15}/>{companyInfo.email}</a><span><MapPin size={15}/>{companyInfo.location}</span><div className="socials" aria-label="Social media placeholders"><span>f</span><span>in</span><span>ig</span></div></div></div><div className="container copyright">© {new Date().getFullYear()} Gentel Elevators. All rights reserved.</div></footer>}
