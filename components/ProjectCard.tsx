import Link from "next/link"; import { ArrowUpRight, MapPin } from "lucide-react"; import { Project } from "@/data/projects"; import { ReferenceImage } from "./Shared";
export function ProjectCard({project}:{project:Project}){return <article className="project-card"><Link href={`/projects/${project.slug}`}><ReferenceImage src={project.coverImage} alt={`${project.title} elevator project`} position={project.imagePosition}/><div className="project-content"><div className="project-meta"><span>{project.category}</span><span>{project.service}</span></div><h3>{project.title}</h3><p>{project.description}</p><small><MapPin/> {project.location}</small><b>View project <ArrowUpRight/></b></div></Link></article>}
