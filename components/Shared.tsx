import Link from "next/link"; import Image from "next/image"; import { ArrowRight, MessageCircle, Phone } from "lucide-react"; import { companyInfo } from "@/data/company";
export function Eyebrow({children}:{children:React.ReactNode}){return <p className="eyebrow">{children}<i/></p>}
export function PageHero({title,description}:{title:string;description:string}){return <section className="page-hero"><div className="container"><Eyebrow>Gentle Elevators</Eyebrow><h1>{title}</h1><p>{description}</p></div></section>}
export function ReferenceImage({alt,position="50% 50%",className=""}:{alt:string;position?:string;className?:string}){return <div className={`reference-image ${className}`}><Image src="/images/design-reference.png" alt={alt} fill sizes="(max-width: 768px) 100vw, 50vw" style={{objectPosition:position}}/></div>}
export function CTA(){const msg=encodeURIComponent("Hello Gentle Elevators, I would like to ask about your elevator services.");return <section className="cta"><div className="container cta-inner"><div><Eyebrow>Get in touch</Eyebrow><h2>NEED AN ELEVATOR SERVICE?</h2><p>Contact Gentle Elevators for installation, repair, maintenance, or a project quotation.</p></div><div className="cta-actions"><a className="button button-whatsapp" href={`https://wa.me/${companyInfo.whatsappDigits}?text=${msg}`} target="_blank" rel="noreferrer"><MessageCircle/>Chat on WhatsApp</a><a className="button button-outline" href={`tel:${companyInfo.phoneDigits}`}><Phone/>Call us</a><Link className="button button-light" href="/contact#quote">Request a quote<ArrowRight/></Link></div></div></section>}
